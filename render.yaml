services:
  - type: web
    name: workshop-platform
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://web-production-6a1e6a.up.railway.app
